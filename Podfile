# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ParcelTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ParcelTracker
  pod 'SwiftSoup'

  target 'ParcelTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ParcelTrackerUITests' do
    # Pods for testing
  end

end
